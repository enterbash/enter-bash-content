#!/bin/bash
# Setup for aiops-structured-logging
set -e
# No special setup needed — app.py is already copied
echo "Ready. Edit ~/app.py to output structured JSON logs, then run: python3 ~/app.py > ~/app.log 2>&1"
