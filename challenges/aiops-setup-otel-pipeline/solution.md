# Solution: Set Up an OpenTelemetry Pipeline

## What the validator checks

- ~/otel-config.yaml not found.

## Solution

Address each check above in order:

**~/otel-config.yaml not found.**
  → 1. Check otel-config.yaml exists

