---
- name: Create app directory
  file:
    path: /tmp/include-test
    state: directory
    mode: "0755"

- name: Create logs directory
  file:
    path: /tmp/include-test/logs
    state: directory
    mode: "0755"
