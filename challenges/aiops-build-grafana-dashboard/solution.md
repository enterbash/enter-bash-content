# Solution: Build a Grafana Dashboard

## What the validator checks

- ~/dashboard.json not found.
- ~/dashboard.json is not valid JSON.

## Solution

Address each check above in order:

**~/dashboard.json not found.**
  → 1. Check dashboard.json exists

**~/dashboard.json is not valid JSON.**
  → 2. Check it's valid JSON

